---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 8-jdk
inputs:
  - name: git-repo
  - name: target
run:
  path: /bin/sh
  args:
    - -c
    - |
      cd git-repo
      ls -lrta
      echo "hello world"
      #java -agentlib:TakipiAgent -jar target/shopping-cart-demo-1.0.jar

caches:
  - path: maven

params:
